### Example environment variables for the backend service
# Copy this file to `backend/.env` and fill in real values before running.
# Do NOT commit secrets to public repos. Keep the real `backend/.env` out of source control.

# URL of the LLM HTTP API (llm stub or external provider). Example default used in code is http://localhost:11434
LLM_API_URL=http://localhost:11434

# Optional: database connection string (if your deployment uses one)
#DATABASE_URL=postgresql://user:pass@localhost:5432/dbname

# Optional: application secret key (used by auth/session code)
#SECRET_KEY=replace-with-a-secure-random-string

# Optional: environment flag
#ENV=development
