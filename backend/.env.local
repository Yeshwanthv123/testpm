### Non-secret local defaults to make the project runnable after cloning.
# This file is safe to commit because it contains no secrets.
# Use this as a starter: copy to backend/.env when you want to run locally.

LLM_API_URL=http://localhost:11434
ENV=development
SECRET_KEY=dev-local-change-me
# If you don't have Postgres locally, the app can use a local sqlite file for quick tests
# DATABASE_URL=sqlite:///./dev.db
